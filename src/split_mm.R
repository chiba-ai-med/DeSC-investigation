# source("src/Functions.R")

# # Argument
# args <- commandArgs(trailingOnly = TRUE)
# size <- args[1]
# infile <- args[2]
# outfile <- args[3]

# # Pre-processing / Output
# 344419908
# .lapply_pb(seq_len(????), function(index){
# 	.skip.read.table(infile, outfile, index=index, nrows=1000, size=size)
# })
